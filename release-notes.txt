Echo 0.1.8 fixes final punctuation after dictation stops.
- When the punctuation model leaves a trailing comma, Echo now replaces it with one final period instead of producing a comma followed by a period.
- Final punctuation still runs only after Stop and respects the Periods setting.
